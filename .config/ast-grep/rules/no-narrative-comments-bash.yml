id: no-narrative-comments-bash
language: bash
severity: error
message: "narrative comment not allowed; only tool-directive comments are permitted"
rule:
  kind: comment
  not:
    regex: "^#!|^#\\s*(shellcheck|yamllint|biome-ignore|eslint-ignore|eslint-disable|nosemgrep|zizmor:\\s*ignore)"
