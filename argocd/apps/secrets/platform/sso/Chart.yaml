apiVersion: v2
name: sso
description: The OIDC client secrets each internal application needs to log in through Keycloak, delivered as SopsSecret next to the consumer and kept in step with tofu/keycloak
version: 0.1.0
