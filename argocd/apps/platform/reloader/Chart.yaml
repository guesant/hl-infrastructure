apiVersion: v2
name: reloader
description: Stakater Reloader, which restarts the workloads that opt in whenever a Secret or ConfigMap they consume changes, so a rotation declared in git reaches the process
version: 0.1.0
dependencies:
  - name: reloader
    version: 2.2.17
    repository: https://stakater.github.io/stakater-charts
